#include<bits/stdc++.h>
using namespace std;

int repeatedNumber(const vector<int> &A) {
    int arrTimeOcc[A.size()];
    for(int i=0;i<A.size();++i)
    {
        arrTimeOcc[i]=0;
    }
    for(int i=0;i<A.size();++i)
    {
        
    }
    return 0;
}

int main(){
    vector<int> inp={1,2,3,4,1};
    cout<<repeatedNumber(inp);
}